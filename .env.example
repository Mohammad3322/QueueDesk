VITE_USE_MOCK=true
VITE_API_BASE_URL=
# Set to a value > 0 (e.g. 0.1) to simulate occasional request failures
VITE_FAILURE_RATE=0
# Generate extra tickets for the 10k performance exercise (see .env.stress)
VITE_STRESS_TICKETS=